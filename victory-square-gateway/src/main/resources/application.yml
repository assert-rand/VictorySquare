spring :
  application:
    name : victory-square-gateway

  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders : "*"

server :
  port : 9003

eureka :
  client :
    serviceUrl :
      defaultZone : http://eureka:8761/eureka